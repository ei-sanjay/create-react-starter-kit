{
<% if (isTypeScript) { -%>
  "preset": "ts-jest",
<% } -%>
  "testEnvironment": "jsdom",
  "setupFilesAfterEnv": ["<rootDir>/tests/setup.<%= ext %>"],
  "moduleNameMapper": {
    "^@/(.*)$": "<rootDir>/src/$1",
    "\\\\.(css|scss)$": "<rootDir>/tests/styleMock.<%= ext %>"
  }
}
